// Deployment-specific config — this is the ONLY file you edit when the
// backend URL changes (local dev vs. deployed). Nothing else references
// a backend URL directly.
const API_BASE = "https://biz-agent.onrender.com";